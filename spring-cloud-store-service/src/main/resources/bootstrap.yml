spring:
  cloud:
    config:
      uri: http://spring.cloud.config.server:8888
      fail-fast: true
  application:
    name: spring-cloud-store-service
logging:
  level:
    root: DEBUG
  path: /tmp
  file: spring-cloud-store-service.log